const navLinks = document.querySelector(".nav-links")
const menuNavBar = document.querySelector(".menu-bar")

function menuBar () {
    menuBar = "clicked"
    if(menuBar = "clicked") {
        navLinks.
    }
    console.log("clicked");
}